# Research Sources

> Indexed documents feeding into this spec. Each tagged with which sections it informs.

- **DOC-1**: `docs/design/README.md` — the original TUI design handoff
  (tokens, copy, glyphs, keybindings) that `kando` was built from. The web
  page's feature set and vocabulary (lanes, tags, checklist, blocked
  reason, archive) come from the same source, so both surfaces speak the
  same language. Feeds: §1, §2, §5.
